# AgroNavis — Python Backend Dependencies
# ========================================
# Run: pip install -r requirements.txt
# Or:  python -m venv venv && source venv/bin/activate && pip install -r requirements.txt

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9     # Required for UploadFile/multipart

# Environment
python-dotenv>=1.0.0

# Supabase SDK
supabase>=2.9.0

# Data validation
pydantic>=2.7.0

# ML — PyTorch (CPU build; use torch+cu118 for GPU)
torch>=2.3.0
torchvision>=0.18.0
Pillow>=10.0.0

# CLIP (OOD guard for non-plant images)
transformers>=4.40.0

# Web search (disease wiki)
duckduckgo-search>=6.2.0

# ONNX Export and Quantization
onnx>=1.15.0
onnxruntime>=1.16.0
onnxscript>=0.1.0
